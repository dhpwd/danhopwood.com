# Blog post ideas

Backlog of potential topics. Not all will become posts – the bar is: would this be worth 5-10 minutes of a stranger's time?

## Ideas

- Fighting bloat in AI-generated content – optimise context docs for AI first, not humans
- Knowledge work with Claude Code – Emma (EA) output style
- I built Granola in 2 hours (`~/dev/recall-recorder`) – open source repo first, then blog post walks through the build with repo as proof. Angle: "replaced a SaaS tool in 2 hours with AI agents". Notes (Feb):
  - **5 improvements over Granola:** (1) all meeting triggers worked reliably, (2) async recording (records locally, no streaming, uploads at end) → model gets whole recording → noticeably better transcription, (3) separates attendees by name vs Granola's "Me vs Them" – critical for 3+ person calls, (4) full control over transcript deployment (webhook or local folder), (5) agent workflow processes each transcript through existing account context, auto-edits frontmatter for filtering (e.g. training tag), archives
  - **Buy vs build reasoning:** used Recall desktop SDK rather than building call detection/recording from scratch. Speaker separation worked out the box. "Super cheap". Trade-off: requires Recall API key (not fully self-contained)
  - **Key detail from 8 Feb:** "context7 doc is key" – shared a doc covering gotchas and how it works. Also: "key thing is it handles multiple participants (and separating them out) vs Granola's Me vs Them, which is confusing"
  - Platforms: web (blog post) + tw + li + wa seed ("wrote up the granola replacement: [link]")
- How to move from Claude desktop app to CLI (Claude Code) for non-technical folks
- How to remain focused – deep work and AI/phone addiction
- The memory bank framework – signature concept, canonical reference post. 6-file structured memory system for persistent AI context across sessions. Source: `~/cli-agents/shared/memory-bank.md`. Angle: "my AI forgets everything between sessions – here's the 6-file system that fixes it". Notes (Feb) – key questions and answers to structure the post around:
  - **"How do I initialise?"** → Prompt "initialise memory bank" with lean canvas or architecture docs attached for richer project-brief/product-context. Agent creates `memory-bank/` folder and all 6 files. For multi-repo: import a single shared file (`@~/my-instructions/memory-bank.md`) instead of copy-pasting
  - **"Meaningful difference vs compress?"** → Compress = lossy summary of a single session. Memory bank = structured, persistent, full-fidelity context across sessions. 5 separate files with update guidelines. Each new session loads all 5 → much richer and more predictable context for continuation than a compressed summary could ever produce
  - **"What about updating?"** → Instructions include an "update memory bank" trigger with steps. Run after each task. On trigger: review ALL files, even if some don't need updates
  - **Stale misleads harder than empty:** the memory bank's failure mode is quiet decay – a session ships work but skips the update, next session opens the files and starts from a confident-but-wrong orientation. Observed on Fidero: after the homepage spec landed, `active-context.md` still said the spec was the next deliverable and `progress.md`'s In progress section still said "awaiting homepage spec work". A fresh agent would read both with the authority of fresh context and waste time re-synchronising before realising the files were out of date. **Sharper framing:** stale context actively misleads – it reads with confidence and creates false orientation. Empty context makes the next session start fresh; stale context makes them start wrong. **Discipline:** memory bank updates belong in the same commit as the work they describe, or an immediate follow-up before session end. The framework's "only update when prompted" rule is about avoiding bloat, not deferring maintenance – prompt at session end, land the update, close out clean
  - **Bloat is the parallel failure mode:** staleness is one decay direction; bloat is the other. Observed on Fidero in the polish pass after Phases 1–3 landed: `progress.md`'s new "Done" bullets ran 5–7 lines each (narrative-leaning) while every older bullet ran 1–3 lines (summary-style); `active-context.md` grew an "Implementation notes" section that duplicated lessons already living in `system-patterns.md`; the same lessons appeared in three places. None of it factually wrong – just over-distributed. The shared `memory-bank.md` already has the rule (`Never duplicate full lessons in both`); the agent (me) violated it anyway and a deliberate polish pass caught it. **Fix:** extract lessons to `system-patterns.md` (their rightful home per the content-boundary rules), leave one-line pointers in `progress.md` and `active-context.md`, delete the duplication. Compression by relocation, not by information loss. **Discipline:** the "review all six files" step on memory bank update isn't just a staleness check, it's a distribution check – does each piece of information sit in its rightful home, or has it leaked into multiple files? The audit is bidirectional. **Mechanism:** agents producing volume default to assuming volume = thoroughness, so the bloat side of decay needs the same active counter-pressure as the staleness side
  - **Session workflow that connects it:** Never compact, never clear. Task done → commit → /exit → new session. /prime loads all memory bank files at start of each session. Compaction becomes unnecessary because sessions stay short and focused, and cross-session context lives in the memory bank not in compacted summaries
  - **The coherence argument:**
    - The obvious pitch is session continuity – "your AI forgets, here's how to fix it." But the stronger argument is architectural coherence
    - Without a bird's eye view, AI builds isolated solutions in-situ: duplicated logic, inconsistent patterns, responsibilities landing in the wrong place. It doesn't even register consistency as a concern – it's wired to complete the task in front of it, not to think about where the solution belongs in the wider system
    - Gets worse as the codebase grows. Each new task adds another isolated solution that doesn't know about the others
    - Two dimensions of code quality: (1) quality in individual pieces of code – AI is good at this, (2) coherence and consistency across the full app – AI is nearly incapable of this _by default_. Memory bank attacks dimension 2
    - The fix: memory bank gives the AI both the bird's eye view (product context, project brief) and the lower-level design conventions and patterns (system-patterns.md, tech-context.md) _before_ it touches anything. "Give it the blueprints first to orient, then give it the spec to execute against"
    - This reframes memory banks from "nice to have for context" to "architectural necessity" – especially for anyone past hobby-stage codebases
    - The continuity argument has a reasonable counter: "just use /compact." Nobody has a counter for the coherence argument
    - Find an analogy for "each piece is good, the whole doesn't hold together." The experience is universal for anyone whose AI codebase has grown – needs something that makes the mechanism click, not just a brand comparison
  - Include: file hierarchy diagram, content boundaries between files, audit triggers, why this + session-per-task = compaction is never needed
  - Platforms: web (canonical reference) + tw + li + wa seed
- Managing AI agent memory across projects – naming decisions, DRY config, diff verification for memory updates
- Structuring agent sessions for complex implementations – plan mode lifecycle (auto-persistence through context clear + compaction), spec vs plan distinction, compaction hooks (what to cover vs what's already handled), memory bank as session bridge. Full workflow with config examples. Source: [[Agentic Coding Best Practices]] + any spec-to-implementation example

- **Rebuilding fidero.com with an AI agent – the reference-doc-driven workflow.** Scaffold + reference doc + memory bank + semantic colour alias for the Fidero marketing site rebuild, starting from the Astroship Pro v2.2.0 template. The repo itself is permanently private so the post IS the public artefact
  - **Angle (KPOI):** This is the fourth attempt at fidero.com. The first three were painful in different ways. The fourth is substantially less painful – not because Astro is magic but because the workflow finally lets an AI agent actually drive the build. The post's hook is the contrast between the three that didn't work and the one that does, and the non-obvious thing that makes the difference (a reference doc the AI can follow). Strapline candidates: _"Rebuilding fidero.com: I've done this four times. Here's what finally worked"_ / _"Three painful attempts and one reference doc"_ / _"Rebuilding fidero.com: one reference doc, two roles, one commit to rebrand"_ (first two are narrative, third is technical-spine)
  - **The journey – four attempts (hook material):**
    - **Umso (manual GUI):** evaluated alongside Webflow and Framer; Umso was the fastest of the three to get something live. "Fastest GUI" still means every piece of copy, every image, every section layout is constructed by hand. Scaling = manual labour per page. Death by a thousand clicks
    - **Astro port of the Umso export (older repo exists separately):** the intent was right – get off GUI, into code, make it AI-maintainable. The execution was agonising. Umso's exported HTML and JS is generated output, not hand-written code, and reverse-engineering it into a clean Astro project took an unreasonable amount of time. Lesson: you can't port a generated codebase into a maintainable one. Start clean
    - **Lovable (React SPA):** currently live at fidero.com. Fast to scaffold, shadcn components, decent visual output. AI _can_ drive this codebase – it's real React code, not generated output like Umso's export. The issue isn't AI-drivability. The issue is architecture: React Router, 60+ dependencies, client-side hydration, SEO penalties that matter, and the perpetual upgrade churn that comes with a dependency tree that size. The principle: **marketing sites should ship as static HTML by default**. React SPAs are the wrong tool for this job even when AI can work with them. **The sharper lesson from this attempt:** AI-drivability is necessary but not sufficient – the architecture still has to be right for the use case
    - **Astroship Pro + reference doc method (this session):** finally. The non-obvious thing that makes this work isn't Astro itself – it's the reference doc. AI can follow it. None of the previous approaches made AI collaboration clean
    - **Journey lesson:** the tool is less important than whether AI can actually drive it. GUI tools can't be driven by AI. Generated HTML can't be reverse-engineered cleanly. React SPAs can be driven but aren't the right architecture for a marketing site. A lean static template + a well-structured reference doc that describes every component in AI-readable terms = leverage. **The reference doc is the missing piece across all prior attempts** – and once that clicks, it retroactively explains why the others failed
  - **Honest timing:** not literally an afternoon. Several focused hours of conversation with meaningful thinking between steps. "One session to scaffold, one reference doc, one commit to rebrand" is more accurate than "one afternoon". Don't oversell or the post loses authority
  - **Core pattern – the reference-doc contract:** Split the work into two roles that can be agent-played in sequence. The reference doc (`docs/component-reference.md`) catalogues every component in the template with the detail a copywriter actually needs: props, slots, **content-shape constraints with specific word-count ranges** (hero headline max 10 words, testimonial quote 25–45 words, card description 15–25 words, FAQ answer 40–80 words), when-to-use vs when-not-to, visual notes, embedded screenshots of how each component renders. Plus design tokens (colour system, type scale, spacing rhythm), six section-level page patterns (landing / features / pricing / about / contact / interior), and a list of known gaps where the template doesn't ship what we need. It's not a component list – it's a contract-grade spec. (1) **Copywriter** – reads the reference doc, picks a section pattern, produces a per-page spec at `docs/specs/[page-name].md` that names specific components and supplies content fitted to their constraints. Copy is drop-in-ready because it was written against the constraints, not discovered at implementation time. (2) **Developer** – reads the spec + the reference doc, implements the page. The reference doc is the contract. Gaps are flagged as `CUSTOM: [component]` so the developer knows when to build rather than force-fit. **Distinctive move:** spending half a day on the reference doc up-front means every subsequent page build is close to one-shot-able. Most "AI codes my website" demos skip this and wonder why the output is generic. **The generalisation:** handoff fidelity is the first thing to collapse in agent workflows; a contract-grade reference doc fixes it for bounded problems – building out pages in a known template – which is narrow but concrete enough to learn from
  - **Reference docs are statements of truth, not logs (editing sub-lesson, part 1 – tone-scrubbing):** While capturing screenshots for the reference doc, the agent wrote the findings as "**Verified:** X is Y". Looked fine in isolation. I pushed back: _"shouldn't the document simply be a statement of the information needed vs a running history?"_ Right – the "Verified:" framing was process history masquerading as reference material, and every such note rots the moment it's written ("verified by whom? when? still true?"). The fix was to rewrite every instance as plain present-tense statements of current truth. **The principle:** reference docs state the present; logs state the past. Mixing them makes the reference doc decay because process notes become stale immediately. **Why this matters for AI-authored docs specifically:** agents naturally produce log-style output because they narrate what they just did ("I verified X", "I checked Y", "I found Z"). Steering them to present-tense statements of standing truth takes deliberate editing. Worth naming as a distinct editing pass when working with agents on reference material – AI-generated reference docs need a "strip the process history" sweep before they're actually usable as reference
  - **Reference docs need a second editing pass – usage-feedback (editing sub-lesson, part 2):** The homepage spec surfaced a second maintenance pass beyond tone-scrubbing. After using the reference doc on a real task (speccing the homepage section by section), the doc had visible blind spots – the scaffold pass couldn't see them because you don't know what you don't know until you've used the thing. Concretely, we went back and added: a new "Starting a page spec" section at the top of `component-reference.md` (scrape workflow, relax-before-new principle, pattern-naming rule, overrun-handling guidance), per-component "Relaxation candidate" notes inline against Hero/Logos/Features/FeatureGrid/Testimonials/CTA (so the next copywriter sees the lessons without re-discovering them), and an updated workflow summary at the bottom with `RELAXED`/`NEW` flag conventions, a two-table output format (Table A relaxations, Table B new components), and a cross-link to `homepage.md` as the first worked example. **The tighter frame:** reference docs need two distinct editing passes – **tone-scrubbing** (part 1) gets you a doc that's correct in isolation; **usage-feedback** (part 2) gets you one that's correct in practice. Scaffold pass produces the first; first real use of the doc produces the input for the second. Worth planning for as a deliberate checkpoint after any non-trivial task the doc is supposed to guide – otherwise the blind spots compound across every subsequent use
  - **Specs need their own review pass – strategic-consistency (editing sub-lesson, part 3):** Reference docs need tone-scrubbing and usage-feedback (parts 1–2 above); page specs need a third checkpoint – review against the memory bank before implementation starts. The homepage spec was scraped verbatim from the live fidero.com, which is fine as a content source, but the live site carries decisions that contradict Fidero's positioning strategy. Concretely: a "Manifesto" nav item that `product-context.md` had already flagged for replacement with an About page ("feels pretentious for Fidero's current stage"), and a "Solutions" dropdown linking to seven vertical and comparison pages whose survival into the rebuild is documented as TBD. Implementing the spec as-written would have shipped a homepage that contradicts strategy on one link and 404s on seven others. Review caught both before any code was touched. **The generalisation:** specs are inputs to implementation sessions, and once handed off they get executed without a human on every line – strategic consistency with the permanent project context (memory bank) is non-negotiable, otherwise the implementation session propagates errors that were invisible at drafting time. **Three editing checkpoints, three different questions:** tone-scrubbing asks _is this written as reference or as log?_ Usage-feedback asks _does this cover the blind spots first real use surfaced?_ Spec-vs-memory-bank asks _does this contradict anything we've already decided?_ Cumulative – each catches errors the others miss, and skipping any one means the post-hoc fix costs more than the pass would have
  - **Don't reproduce what the tool surface already provides (editing sub-lesson, part 4 – deduplication):** Parts 1–3 are about how to edit reference material. Part 4 is about _what belongs in it_ when the subject is an externally-defined thing with its own surface – a tool, an API, a schema. First draft of the memory bank's Stitch notes duplicated the tool schemas themselves: tool names, parameter descriptions, enum values, operational warnings – despite the next session loading all of that on demand via its own tool flow. Caught, trimmed. Second round still had meta-instruction redundancy ("Load schemas via ToolSearch before first use") – the harness already enforces this. Caught again, trimmed again. Third round finally converged on value-add only: **interpretation** (Stitch works in screens, not components), **project-specific values** (what to set colorMode/fonts/roundness/customColor to for _this_ template), **workflow integration** (Phases 4–5 only, unit-mismatch workaround), and **non-obvious watch-outs** (apply*design_system is a retrofit, not a generation hook; output format unknown until first run). **The principle:** reference material about an externally-defined thing should carry only what that thing doesn't describe itself. Everything else is accessible the moment the reader reaches for it. **Mechanism:** agents over-document because documenting feels like progress. Enumerating known facts is easy; omitting in favour of distilled interpretation is harder. The default is over-write, and the counter-pressure is to keep asking: \_does the reader already have access to this?* **Same structural beat as tone-scrubbing (part 1):** both are forms of over-writing reference material. Tone-scrubbing includes process history that belongs in logs; deduplication includes tool facts that belong in schemas. Same corrective question in both cases
  - **Framework-drift check as editorial pass (editing sub-lesson, part 6 – external alignment):** Parts 1–5 are all internal editing passes – tone, blind spots, strategic consistency, deduplication, structured cleanup. Part 6 is the external pass: any reference material that wraps a third-party framework (Astro components, Tailwind config, content collections, image pipeline) needs periodic re-validation against that framework's current docs, because the framework moves under your feet between the doc-write moment and any future read. Worked example: user-prompted _"is there anything in the component reference doc or the homepage spec worth checking against the Astro documentation (via the Astro MCP)?"_ Six real findings in <15 minutes against the Astro docs MCP. (1) `src/content/config.ts` was riding an automatic Astro 5 backcompat path that disappears in Astro 6.0, with a substantive consequence: blog featured images were typed as `z.string()` and consequently bypassing `astro:assets` despite the template using `<Picture>` – the `image()` schema helper is the fix, but until run, every blog post's hero image was emitted unoptimised. (2) The `image.layout: 'constrained'` line in an existing backlog entry was missing its required pair `image.responsiveStyles: true` – without both, you get the responsive markup but no resizing styles, which is the pit of failure. (3) `<ViewTransitions />` was renamed to `<ClientRouter />` in 5.0 and removed in 6.0; the `tech-context.md` framework description still used the old name. (4) The marquee primitive's `Astro.slots.render() + set:html` pattern is sanctioned but flagged "advanced use case" in the docs, with no listed caveats around hydration metadata, scoped styles, or ID uniqueness – the constraints captured in `system-patterns.md` were inferred not sourced, worth flagging as such. (5) The ContactForm replacement backlog listed Calcom and routing forms but not Astro Actions (`astro:actions`, added 4.15.0), the framework-native path that keeps the form on-domain and matches the project's voice rules better than an embed. (6) The blog `[slug].astro` used legacy entry API (`params: { slug: entry.slug }`, `await entry.render()`) that the v5 Content Layer migration explicitly removes – concrete deltas needed before the Astro 5→6 upgrade can land, none of which would have been caught until the upgrade itself failed. **Principle:** memory banks and reference docs accumulate framework knowledge that was correct at write-time and treated as ground truth thereafter. The framework's own docs are the source of truth and should be the periodic check, not the cached summary. **Mechanism:** the cached summary _feels_ authoritative because it's local, project-specific, and was correct once. Re-validating it requires a deliberate pass against an external surface that contradicts the convenience of the local copy. **Pairs with part 4 (deduplication):** part 4 says don't duplicate what the tool surface already provides at write-time; part 6 says periodically re-check what you've cached against what the tool surface says now. Deduplication prevents bloat; framework-drift checks prevent staleness. **Tool gate – good MCPs lower the cost to near zero:** these six findings were gated on the Astro docs being available as an MCP. Without it, the same check would have meant context-switching to a browser, scanning release notes, cross-referencing version-gated features – minutes per finding instead of seconds. The economic claim is concrete: framework-drift checks should be done more often than they are, _because_ a good MCP collapses the cost. **Trigger:** any session that touches framework code, or as a periodic standalone pass. **Connects to the dark corollary below:** the same session surfaced two new failure modes that don't fit the original relay-confidence frame – source-vs-summary substitution and list-write-without-resort – generalising the dark corollary itself
  - **Cleanup pass as institutional editorial pressure (editing sub-lesson, part 5 – simplification):** Beats 1–3 (Sharp / relax / name-by-pattern) require live user push-back; the dark corollary observes that pattern capture alone doesn't prevent recurrence. The constructive answer is **structured self-review passes**, not user vigilance in the moment. Worked example: ran `/simplify` after the Phase 1+2 implementation diff (Marquee primitive extraction + five Astroship component relaxations), which spawns three parallel review agents (reuse / quality / efficiency) against the changed files. They collectively caught ten distinct architectural defaults the working agent had slipped through under "make it work" pressure – gradient-text inlined in three places (extracted to a shared `gradient-text.astro` primitive), CTA dark/light variants scattered across six ternaries (collapsed to a single `variants[background]` map), Hero `ctaLayout: "dual" | "single-with-trust-row"` mode flag coupling two orthogonal concerns (dropped in favour of inferring presence from `secondaryCta != null` and `trustItems?.length`), leaky `gapClass`/`paddingClass` string props on the Marquee primitive (removed), triple-nested logos rendering ternary (flattened), stale `{" "}` artefacts in feature markup, `<Fragment>` workarounds left over from earlier ternary shapes, missing `inert` on the marquee clone track, hand-rolled footnote normalisation, and stringly-typed mode flags. None were bugs; none affected the build. Left unaddressed, the codebase leaks architectural choices that should've been cleaned up, and the leakage compounds with every subsequent feature. **Pairs with beat 2 (relax-before-build):** beat 2 is editorial pressure at _spec time_ – pick the right approach before writing code; beat 5 is editorial pressure at _code-review time_ – catch the wrong-local defaults that slipped through anyway. Both necessary; neither sufficient on its own. Beat 2 catches things the spec author defaulted to wrong; beat 5 catches things the implementer defaulted to wrong despite the spec being right. **Sharper version:** the /simplify pass treats your own previous output as suspect – the same skepticism you'd apply to someone else's code. Agents producing volume default to assuming volume = thoroughness; structured self-review is the institutional counter-pressure. **Generalises:** any sufficiently large agent-driven implementation pass should be followed by a structured review pass against the diff, not just a build/test verification. Build passes confirm the code runs; review passes confirm the code is shaped right. Different question, different answer, different agent
  - **Cross-diff review on substantive edits (editing sub-lesson, part 7 – lossy-transformation check):** Parts 1–6 all evaluate the _current state_ of a document – is the tone right, does it cover real-use blind spots, does it contradict memory bank, does it duplicate the tool surface, does it leak wrong-local defaults, is it stale vs framework docs. Part 7 is structurally different: it evaluates the _current state against the previous state_ during transformation. Worked example from Phase 4 closeout: the lockstep pass on `component-reference.md` replaced five "Relaxation candidate:" proposal paragraphs with shipped prop signatures across Hero/Logos/Features/Testimonials/CTA. Each transformation was correct in isolation – the new entry documented the real props correctly, the new structure read cleanly. What it dropped was two pieces of standing guidance that had been baked into the old paragraphs but weren't part of the relaxation proposal itself: Hero's _"no centred or full-bleed variants – if you want either, that's a custom build"_ and CTA's _"the component only supports one button – for dual-CTA sections use Hero instead"_. Both still valid, both orphaned during the structural rewrite because the new paragraph was shaped around the props, not around the incidental constraints. Neither the lockstep pass nor the polish pass (parts 1–6 applied) caught them – they were scanning _"is the new version correct"_, not _"what did the old version carry that the new version doesn't"_. User prompted the catch explicitly: _"Use git to review your net changes to the reference doc and confirm that we have both captured everything and that no important detail was lost."_ Running `git diff HEAD -- [file]` surfaced both drops immediately; both got restored into the new structure (Hero's visual notes bullet, CTA's `button` prop description with a Hero cross-reference). **Principle:** substantive document edits – replacing a paragraph with a different-shaped paragraph, restructuring a section, folding multiple bullets into one – are _structurally lossy by default_. The new version gets evaluated for fitness-for-purpose; the old version's incidental content gets left behind unless deliberately carried over. **Mechanism:** when the edit shape is "replace A with B that serves a different purpose," the agent focuses on B's fitness-for-purpose and doesn't notice that A carried content beyond the purpose being replaced. The other six passes can't catch it because they all scan current state, not transformation delta. **Discipline:** after any substantive edit pass on a reference doc, run `git diff HEAD -- [file]` before the commit and ask explicitly _"what did the original say that the new version doesn't?"_ – different direction, different scan from the _"is the new version correct"_ question. Cheap to run, catches a class of errors the other passes miss by design. **Pairs with every other pass:** parts 1–6 evaluate current-state; part 7 evaluates transformation delta. Both necessary on substantive rewrites, neither sufficient alone
  - **The semantic colour alias pattern:** Astroship ships indigo-dominant. Renaming `indigo-*` classes to match a new brand colour in-place is the wrong move – it changes what the classes _mean_ without changing the visual. Better: define `brand` in `tailwind.config.cjs` as `colors.brand = colors.indigo`, then find-and-replace `indigo-` → `brand-` across every `.astro` file and rename the variant prop unions in Card/Pricing/Badge. Rebrand = one config edit. Code stays honest. Worth diagramming in the post
  - **The Sharp gotcha – agent-steering beat (1 of 3):** While writing `tech-context.md`, the agent described Sharp as an explicit dev dependency the Astroship template installs. I pushed back – image optimisation "just worked" on danhopwood.com and I didn't remember ever installing Sharp manually. The agent spawned a doc-researcher sub-agent against docs.astro.build, which came back confident: Sharp is Astro's default image service but "not bundled with Astro itself – listed as a peer/dev dependency that the project must provide." The main agent relayed that with confidence. I pushed back again. The agent then checked `node_modules/astro/package.json` directly – Sharp sits in `optionalDependencies`, meaning it auto-installs transitively unless the install fails. The sub-agent had misread the docs. **Lesson:** agents are persuasive at the confidence layer and soft at the ground-truth layer. When a claim is one `grep` away from certainty and the user's intuition disagrees, always ground-truth-check before relaying. _"Astro ships image optimisation out of the box, zero manual Sharp install needed in normal environments"_
  - **Relax before build – agent-steering beat (2 of 3):** First draft flagged 12 new components. User push-back: _"is it possible that some of them can be achieved by updating existing components? e.g. 'locked at 6' – we could update the component to make it more flexible, which feels better than creating a new component"_. Reassessed. Five of the 12 were really prop-level relaxations to existing components (Hero → named right-side slot + single-CTA-with-trust-row variant, Logos → variable count + optional marquee prop, Features → variable count + optional role-label + longer descriptions, Testimonials → variable count + `layout="marquee"|"grid"` prop, CTA → optional bullets + footnote + `background="dark"|"light"` variant). Each relaxation additive, stock behaviour kept as default, future pages inherit the flexibility for free. Count went from 12 new → 8 new + 5 relaxations. **Lesson:** agents default to "build parallel component" when "add a prop to the existing one" is cleaner and costs less architecturally. The signal is a spec with a suspiciously long list of customs – check whether each one is one prop change away from fitting. Same structural beat as the Sharp gotcha: plausible-but-suboptimal default, user intuition disagrees, re-check grounds the right answer. **Single-use exception surfaced during spec review:** the same draft proposed code edits to Navbar and Footer – drop the log-in link, remove the template wordmark, widen footer to 4 columns, replace hardcoded link arrays – without flagging them as Table A relaxations. Review raised the ambiguity: either these belong in Table A (prop-ify, earn reusable flexibility) or they don't (in-place content replacement per the hardcoded-content pattern). User closed the gap: _"the navbar is one component where flexibility isn't really important as it's only ever used once"_. Correct – **relax when there's a second caller, edit in place when there isn't.** Relaxing single-use components adds prop surface area with zero reuse payoff; the discipline cost is real, the benefit is zero. Single-use components skip Table A entirely and get treated as straight content replacement – an explicit escape hatch to the relax-before-build default when the stretching itself costs more than it saves
  - **Name by pattern, not instance – agent-steering beat (3 of 3):** Immediately after the relax-vs-build push-back: _"make sure new components are designed so they're reusable. 'tabbed-example-viewer' sounds specific to viewing examples versus being a tabbed component for any situation"_. Correct – first draft had `tabbed-example-viewer`, `topology-diagram-3col`, `declarative-vs-imperative-card`, `step-timeline-3col`, all named after the specific homepage content that motivated them. Renamed to `tab-panel`, `flow-diagram-3col`, `comparison-card`, `step-timeline`. Pattern is generalisable; each homepage section is an instance. **Mechanism:** agents name things after the task they just completed, not the reusable pattern. They're wired to complete what's in front of them, not to hoist the abstraction. Steer explicitly
  - **Three wrong-local defaults, one editorial pressure (meta-frame across beats 1–3):** Sharp = trust the doc over intuition (wrong-local on a factual claim). Relax-before-build = build parallel instead of modifying existing (wrong-local on architectural choice). Name-by-instance = name after the thing in front of you, not the reusable pattern (wrong-local on abstraction level). Three different failure categories, same editorial counter-pressure. Might be the post's spine: agents default to locally-optimal, user steers to globally-optimal, and the non-obvious thing is that you have to steer on architecture and abstraction – not just on facts
  - **Dark corollary's failure modes are diversifying (extension):** Beats 1–3 + the original dark corollary frame the recurrence problem as wrong-local defaults around _confident relay_ – Sharp/relax/name are all variants of "trust the upstream summary instead of ground-truth-checking". A memory-bank sanity-check session (the same one that produced part 6 above) surfaced two new failure shapes that don't fit the relay-confidence frame, and the Phase 4 closeout added a third: **(a) Source-vs-summary substitution.** The user explicitly named two docs as the check target – `docs/component-reference.md` and `docs/specs/homepage.md` – and asked whether anything in them was worth checking against the Astro docs MCP. The agent (me) read homepage.md in full but only partially read component-reference.md, leaning on the memory bank's component-by-component summary for the rest. The user caught it: _"Did you read those docs? Those were the two docs I was referring to."_ A forced second pass through the full reference doc surfaced two findings the first pass had missed (Astro Actions as a contact form replacement option, and the concrete blog `[slug].astro` migration deltas). The failure isn't relaying upstream confidence – it's _substituting the memory bank's summary for the source material the user explicitly named_. Memory bank is a navigation aid; in an editorial context where the user named the doc as the check target, it becomes a confidence trap that hides parts of the source from the editor. **Discipline:** when the user names a doc, read the doc – the memory bank is not a substitute, even when its summary feels comprehensive. **(b) List-write-without-resort.** Same session uncovered backlog ordering bugs that had been wrong across multiple sessions: the Content Layer migration sat _below_ the Astro 5→6 upgrade despite the entry text saying "prerequisite for the upgrade above"; the marquee polish item sat under "Blocked/deferred" despite explicitly being "Phase 6" wire-up work; six items were duplicated across `active-context.md` and `progress.md` because both files were being appended to independently. None caught until the user prompted _"are the backlog items in the correct order?"_ The failure isn't relay-confidence – it's _list maintenance_: agents append to lists in the order they think of items, dependencies don't get re-checked when new items land, and lists grow without periodic re-sort even when the entry text spells the dependency out in plain English. **Discipline:** any session that adds to a backlog/decision list should do a quick "is this still ordered correctly, and does it belong in this file vs the sibling backlog?" pass on the items adjacent to the new entry. **(c) Convention inheritance without audience check.** The Phase 4 closeout uncovered a third shape: `docs/component-reference.md` was using Obsidian-style `[[Wikilink]]` syntax for all its cross-references (75 of them), which I'd followed during the lockstep pass without questioning. The assumption was that the existing syntax was the convention to maintain. User pushback: _"Is there a reason you have adopted a wikilink convention? Is that normal and typical for connecting documents in a codebase? I use Wikilinks for my vault which I open in Obsidian. But I don't view any of the documents in codebases in Obsidian."_ Nothing in the repo ever gets viewed through Obsidian – docs are read through GitHub, IDE previews and AI-agent markdown readers, none of which render wikilinks (they display as literal `[[...]]` text). The "convention" had been inherited from an earlier session that chose it without an audience check, and I inherited it again transitively by continuing it. Fix cost was trivial (75 links converted in a single scripted Python pass, build clean afterward); the cost of _noticing_ was the entire overhead. **Discipline:** on any session that picks up an existing doc with an inherited convention, verify the convention matches the actual viewing environment – especially format choices (link syntax, heading style, code-fence languages, footnote conventions) where the surface form matters. The failure isn't relay-confidence or list-maintenance – it's _fitness-for-audience re-check on a cached format convention_.

**All three shapes share the cumulative-failure quality** with the original Sharp-style instances: invisible until prompted, surviving across sessions, fix is a deliberate periodic pass not a one-time intervention. **Updated frame – sharper than the original dark corollary statement:** the failure isn't only about wrong-local defaults around _facts_; it's about any pattern where agent attention defaults toward the convenience of cached state and away from re-validation against fresh ground truth. Source-vs-summary substitution, list-write-without-resort, convention inheritance without audience check, and the original Sharp-style relay-confidence are four instances of the same deeper pattern – cached state feels authoritative, fresh validation feels like wasted motion, and the agent defaults to the cheaper read every time. The cached state varies by shape (claim / source file / list order / format convention), but the corrective is the same across all four: structured periodic re-validation against the canonical source (file, list state, framework docs, audience fit), prompted by deliberate editorial passes rather than user vigilance in the moment
  - **Stitch production runs – the spec discipline transferred (positive companion to the dark corollary):** Three sessions in, after the dark corollary above had captured "guessing Stitch tool schemas from tool names" and "guessing Stitch theme values from plausibility" as recurring failures, Stitch finally got real production runs – three by Phase 4 close, generating `silent-failure-illustration`, `flow-diagram-3col` and `comparison-card` for the homepage. All single-attempt, all 3.1 Pro, all near-pixel-perfect off the same disciplined spec → prompt handoff. The interesting thing is _why_ – it isn't that the agent suddenly stopped defaulting wrong, it's that the structural correctives finally caught up. Three things landed before the run that hadn't been in place for the prior failures: (1) a Stitch-specific Tooling section in `active-context.md` with template-baseline `create_design_system` values, project/asset IDs, and explicit "what's not obvious from the schemas" notes – loaded automatically by `/prime` at session start, so the next agent inherits the lessons without re-discovering them; (2) the reference-doc-driven copywriter spec for the homepage already existed at section-level granularity, so the Stitch prompt could be assembled by lifting exact text strings, exact card structure, and exact visual intent from the spec rather than improvised in the moment; (3) the prompt itself was treated as a downstream of the spec, not a standalone artefact – same discipline as the copywriter→developer handoff, just one more consumer in the chain. **The generalisable pattern:** the spec discipline scales beyond the original copywriter→developer handoff to copywriter→Stitch→developer (and presumably any other code-or-design-generation tool you slot into the workflow). Stitch is just another reader of the contract – the contract still does the work. **The non-obvious surprise:** the model selection lever isn't exposed in any default. The MCP's `generate_screen_from_text` defaults `modelId` to unspecified, which maps to Flash; the Stitch UI also defaults to Flash; the actually-good model ("Thinking with 3.1 Pro" in the UI / `GEMINI_3_1_PRO` in the schema) only gets used if you go looking for it. First-time callers will silently get the worse model unless they audit the schema or check the UI. Worth surfacing in the post as a watch-out – good MCP usage isn't just "use the tool", it's "audit the schema for quality levers the wrapper doesn't surface". **And one residual translation step:** Stitch's `designMd` steering influences visual vocabulary but not implementation choices like icon library – the output used Material Symbols Outlined despite the designMd asking for fluent. A small Material Symbols → astro-icon swap is part of the translation pattern now and went into the memory bank as a fixed step. **Connects back to the dark corollary:** pattern capture eventually pays off, but only when the captures get embedded structurally (memory bank notes the next session loads; verification steps baked into the workflow; spec discipline that scales to new consumers) rather than relying on the writer to remember in the moment. The corollary says "knowing a pattern is no defence against re-entering it under volume" – the repeated Stitch success doesn't refute that, it shows the only path that does work: making the pattern structural so it doesn't depend on remembering. **N=3 strengthens the claim:** one clean run could be luck; three in a row (across distinct component shapes – single-use illustration, reusable flow topology, before/after comparison card) is the pattern working structurally. The first run's lessons got embedded into `active-context.md#Tooling` immediately, and the next two runs inherited them via `/prime` at session start without re-discovering any of the Phase 4 tooling gotchas. **Beat shape for the post:** dark corollary as the problem (capture alone fails), repeated Stitch success as the answer (capture + structural embedding succeeds, N=3 proof). The two need to land together
  - **Dark corollary – pattern capture doesn't prevent recurrence:** Between capturing beats 1–3 and closing out the session, three more default-recurrences of already-captured patterns appeared – two Sharp gotchas (guessing Stitch tool schemas from tool names; guessing Stitch theme values from plausibility) and one tone-scrubbing violation (writing "verified:" framings in memory bank despite the existing part-1 rule). All three occurred after the corresponding patterns were already documented in this very file. Pattern capture is necessary but not sufficient – the defaults still pull at the writer in the moment and need active counter-pressure from the user. Implication for the post: the editorial counter-pressure isn't a one-time intervention, and knowing a pattern is no defence against re-entering it under volume. The spine is harder than "steer once" – it's "keep steering, every time". **Two more instances surfaced in a follow-on session** during an image-optimisation audit triggered by `use astro mcp to confirm image optimisation is set up per best practices`: (1) misread `awk` output and confidently claimed `sharp` was in `dependencies` when it was actually in `devDependencies` – a re-verification prompt from the user caught it; (2) framed the explicit `^0.33.2` pin as "actively holding sharp back" when Node's closest-wins module resolution means Astro uses its own nested `node_modules/astro/node_modules/sharp` copy regardless of the top-level pin – caught only on a second user-prompted re-check. Both within a single agent pass, both during work the user explicitly asked to be careful about, both restating the same Sharp/ground-truth gotcha from beat 1. The dark corollary is **cumulative – every session adds evidence**, and the audit-style instruction (`use astro mcp to confirm…`) is no defence either: structured verification triggers can themselves become the surface where the wrong-local defaults reassert
  - **Memory bank + CLAUDE.md handoff:** End of session, we wrote a 26-line CLAUDE.md (imports the shared memory-bank instructions) and pre-populated all six memory bank files with real content from the scaffold decisions – not stubs. The next session can pick up cold and be productive immediately. Connects to [[#Memory bank]] idea above – this is a worked example of pre-population at end of a scaffold session rather than initialising from an empty state
  - **What's the post missing?** A finished rebuilt site. As of the scaffold session, zero pages have been rebuilt – only the template with renamed colour classes. The post should be written **after** homepage + remaining pages land + cutover from the Lovable site. Screenshots of the live fidero.com close the story
  - **Structural sketch:**
    1. Hook: I've built fidero.com four times. Three were painful in different ways. Here's what finally worked
    2. Brief tour of the three that didn't (Umso, Astro-from-Umso-export, Lovable) and what each taught
    3. The two-role split – copywriter and developer roles, reference doc as contract. The thing every prior attempt was missing
    4. Worked example: the semantic colour alias – one commit, zero component edits
    5. The Sharp gotcha (agents over-confident, ground-truth matters) – a detour along the way
    6. Payoff: homepage rebuilt in [X minutes] once the spec was written. Live at fidero.com
  - **Platforms:** web (canonical) + tw (workflow thread) + li (business-buyer cut on "why reference docs beat prompt engineering") + wa seed (AI Prompts group, implementation detail)
  - **Images to capture at draft time (~10 min):** reference-doc structure, brand alias one-commit diff, Sharp `optionalDependencies` grep output, memory bank file tree, lean CLAUDE.md, dev server boot time, before/after pages (needs rebuild), git history progression (needs rebuild)
