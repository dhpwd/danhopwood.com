---
interface Props {
  compact?: boolean;
}

const { compact = false } = Astro.props;
---

<div
  class:list={[
    "newsletter-signup",
    { "mt-8 border-t border-border pt-8": !compact },
  ]}
>
  <h2
    class:list={[compact ? "text-lg font-semibold" : "text-xl font-semibold"]}
  >
    Build Notes
  </h2>
  <p class="mt-1 text-sm opacity-80">
    Weekly roundup from building Fidero on agentic workflows. Zero fluff.
  </p>
  <form
    action="https://buttondown.com/api/emails/embed-subscribe/dhpwd"
    method="post"
    class="mt-3 flex gap-2"
  >
    <input type="hidden" name="tag" value="website" />
    <input
      type="email"
      name="email"
      placeholder="you@example.com"
      required
      class="flex-1 rounded border border-border bg-background px-3 py-2 text-sm text-foreground placeholder:opacity-50 focus:border-accent focus:outline-none"
    />
    <button
      type="submit"
      class="rounded bg-accent px-4 py-2 text-sm font-medium text-white hover:opacity-90"
    >
      Subscribe
    </button>
  </form>
</div>
